<h3>Здесь будет список радиостанций с формой поиска и пагинацией</h3>
<style> 
            .geeks { 
                width:60px; 
                height:60px; 
            } 
            img { 
                width:100%; 
                height:100%;
                border-radius:20px; 
            } 
        </style> 
<table>
  <tbody>
    <% @stations.each do |station| %>
      <tr style='width:100%;'>
        <td><div class="geeks"><%= image_tag station["favicon"] %></div></td>
        <td><h2><%= truncate(station["name"], length: 15) %></h2></td>
        <td><%= link_to 'Listen', station["url"] %></td>
      </tr>
    <% end %>  
  </tbody> 
</table>
<div>
  <%= (link_to 'Previos', controller: "pages", action: "index", page: @page - 1) if @page > 1 %>
  <%= link_to 'Next', controller: "pages", action: "index", page: @page + 1 %>
  <h4><%= "#{(@page - 1) * 8 + 1} - #{@page * 8} из #{@total}" %></h4>
</div>
